run_as shiny;
preserve_logs true;
log_dir /var/log/shiny-server;

server {
  listen 3838;

  location /atlas {
    app_dir /srv/shiny-server/atlas;
    log_dir /var/log/shiny-server;

    # Pass environment variables to R processes
    env_var ANTHROPIC_API_KEY;
  }
}

